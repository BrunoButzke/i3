# Copie para .env e ajuste se necessário
# cp .env.example .env

# --- Docker Compose (app + db) ---
POSTGRES_PASSWORD=i3244466666
POSTGRES_PORT=5433
APP_PORT=3000

# URL usada pelo app dentro do Docker (service name: db)
DATABASE_URL="postgresql://i3database:i3244466666@db:5432/i3?schema=public"

# --- Só Postgres no Docker, Next rodando no host (npm run dev) ---
# DATABASE_URL="postgresql://i3database:i3244466666@localhost:5432/i3?schema=public"

# Importação do Excel (volume ../appScript no compose)
APPSCRIPT_DATA_PATH=/data/appScript

# Rode import no startup do container: RUN_DB_IMPORT=true
# RUN_DB_IMPORT=false
